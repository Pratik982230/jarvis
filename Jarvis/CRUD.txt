# ===============================================
# 🗃️ SQLite - CRUD Operations with User Input
# ===============================================
import sqlite3

con = sqlite3.connect("student.db")
cur = con.cursor()

# Create Table
cur.execute("CREATE TABLE IF NOT EXISTS student(id INTEGER PRIMARY KEY AUTOINCREMENT, name TEXT, age INT)")

# Insert Data
name = input("Enter student name: ")
age = int(input("Enter student age: "))
cur.execute("INSERT INTO student(name, age) VALUES(?, ?)", (name, age))
con.commit()
print("✅ Data Inserted!\n")

# Read Data
print("📘 All Students:")
for row in cur.execute("SELECT * FROM student"):
    print(row)

# Update Data
sid = int(input("\nEnter student ID to update age: "))
new_age = int(input("Enter new age: "))
cur.execute("UPDATE student SET age=? WHERE id=?", (new_age, sid))
con.commit()
print("✅ Data Updated!\n")

# Delete Data
sid = int(input("Enter student ID to delete: "))
cur.execute("DELETE FROM student WHERE id=?", (sid,))
con.commit()
print("🗑️ Data Deleted!\n")

# Show Final Data
print("📘 Final Student Records:")
for row in cur.execute("SELECT * FROM student"):
    print(row)

con.close()
